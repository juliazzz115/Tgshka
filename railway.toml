[build]
builder = "NIXPACKS"

[deploy]
startCommand = ". /opt/venv/bin/activate && cd telegram_swiper_web/backend && gunicorn --worker-class=eventlet --workers=1 --bind 0.0.0.0:$PORT app:app"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
