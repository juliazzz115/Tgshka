[build]
builder = "NIXPACKS"
buildCommand = "pip install -r telegram_swiper_web/requirements.txt"

[deploy]
startCommand = "cd telegram_swiper_web/backend && python app.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
